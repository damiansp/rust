fn main() {
    let x = gcd(1302, 462);
    let room = player.location();
    let mut numbers = Vec::new();
    server
        .bind("127.0.0.1:3000")
        .expect("error binding to address")
        .run()
        .expect("error running server");
    //return Vec<i32>::with_capacity(1000);  // error parsing < 
    return Vec::<i32>::with_capacity(1000);  // OK
}